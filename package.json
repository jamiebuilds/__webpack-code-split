{
  "name": "webpack-code-split",
  "scripts": {
    "build": "rm -rf dist && webpack --config webpack.conf.js --progress --colors",
    "dev": "rm -rf dist && webpack --config webpack.conf.js --progress --colors --watch"
  },
  "devDependencies": {
    "globby": "^4.0.0",
    "webpack": "^1.12.14"
  }
}
